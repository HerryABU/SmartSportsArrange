# ============================================================
# SmartSportsArrange - .gitignore
# ============================================================

# ---------- Build artifacts ----------
# Maven (backend)
sports-backend/target/
target_old*/
*.class

# Node / Vite (frontend)
sports-frontend/node_modules/
sports-frontend/dist/
dist_old*/

# 前端构建产物（已同步至后端静态资源目录，构建时生成，不纳入版本控制）
sports-backend/src/main/resources/static/

# ---------- 工作区本地数据（记忆/技能等，不提交） ----------
.workbuddy/
sports-frontend/.vite/

# Packaged executable JAR (regenerated by build.ps1)
*.jar
sports-1.0.0.jar

# Original project skeleton archive (superseded by current source)
starter.zip

# ---------- Runtime data & local database ----------
# Embedded SQLite DB (auto-recreated on first launch)
sports_meet.db
sports_meet.db-shm
sports_meet.db-wal

# Runtime data directories - ignore runtime contents, keep folder structure
data/logs/*
data/exports/*
data/uploads/*
data/backup/*
data/avatars/*
!data/**/.gitkeep
*.log

# ---------- Secrets / environment ----------
.env
.env.*
*.local

# ---------- IDE ----------
.idea/
.vscode/
*.iml
*.swp

# ---------- OS ----------
.DS_Store
Thumbs.db
ehthumbs.db
